spring:
  application:
    name: hrmanagementsystem

  datasource:
    url: jdbc:mysql://localhost:3306/hrms
    username: root
    password: SEZee26
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
     ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

  mail:
    host: smtp.gmail.com
    port: 587
    username: ${MAIL_USERNAME}
    password: ${MAIL_PASSWORD}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true


server:
  port: 8085


jwt:
  secret:  mySuperSecretKeyThatIsAtLeastsooBitsLongForHS256Algorithm
  expiration: 86400000 #24 hours

